import { defineConfig } from 'astro/config';

// TECHMED web foundation — static output, deployed to Cloudflare Pages.
export default defineConfig({
  site: 'https://techmedng.com',
  output: 'static',
});
