---
// Student proof section, per the "Built to Last" refinement brief.
// Placed after WhyTechmedExists (the problem/system explanation) so proof
// follows the pitch, ahead of the deeper Builder/ecosystem content.
//
// The hero testimonial is condensed from a real message from TECHMED's
// 2026 UTME top scorer (372) — verbatim source material, not rewritten
// marketing copy. Do not add a name, photo, or embellishment beyond what
// was verified: the brief deliberately attributes it as "2026 UTME Top
// Scorer" rather than by name.
---

<section class="section proof">
  <p class="eyebrow">Student Proof</p>
  <h2>Built With Students. Proven By Results.</h2>

  <div class="testimonial reveal">
    <blockquote>
      <p>&ldquo;Your tests and podcasts have been helpful.&rdquo;</p>
      <p>&ldquo;I've seen my result and I'm really proud to have you tutor me&hellip; you're one of the people I look up to.&rdquo;</p>
    </blockquote>
    <cite>&mdash; 2026 UTME Top Scorer, 372</cite>
    <p class="supporting-line">372 in UTME 2026. Prepared within the TECHMED ecosystem.</p>
  </div>
</section>

<style>
  .section {
    max-width: 720px;
    margin: 0 auto;
    padding: var(--space-20) var(--space-6);
  }

  .eyebrow {
    font-family: var(--font-display);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--accent-text);
    text-align: center;
    margin-bottom: var(--space-4);
  }

  h2 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    text-align: center;
    margin-bottom: var(--space-10);
  }

  .testimonial {
    background: var(--surface);
    border: 1px solid var(--border);
    border-left: 3px solid var(--accent);
    border-radius: var(--radius-lg);
    padding: var(--space-8) var(--space-8);
  }

  blockquote p {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: clamp(1.1rem, 2.5vw, 1.35rem);
    line-height: 1.6;
    color: var(--text);
    margin-bottom: var(--space-4);
  }

  blockquote p:last-child {
    margin-bottom: 0;
  }

  cite {
    display: block;
    font-style: normal;
    font-family: var(--font-display);
    font-weight: 700;
    font-size: 0.9rem;
    color: var(--accent-text);
    margin-top: var(--space-6);
  }

  .supporting-line {
    font-size: 0.85rem;
    color: var(--text-muted);
    margin-top: var(--space-2);
  }

  @media (max-width: 560px) {
    .testimonial {
      padding: var(--space-6) var(--space-5);
    }
  }
</style>
