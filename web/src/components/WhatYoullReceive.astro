---
// Source: Campaign Operating Manual, Chapter 39 — Landing Page Structure,
// "What You'll Receive," cross-referenced with Chapters 41–44 for how each
// piece actually works.
const items = [
  {
    title: 'Community',
    description:
      "A Builder Cohort — your academic home inside TECHMED. Not a public group. An assigned classroom where you attend sessions, ask questions and meet fellow Builders.",
  },
  {
    title: 'Study Strategy',
    description:
      'Blueprint Tuesday: study planning, timetables and learning systems built around how students actually improve — not generic advice.',
  },
  {
    title: 'Builder Sessions',
    description:
      'The core academic lesson each week — concept breakdowns, past question analysis, and problem solving across your subjects.',
  },
  {
    title: 'Challenges',
    description:
      'Practice days built around questions, quizzes and mocks that sharpen speed, accuracy and exam confidence through repetition.',
  },
  {
    title: "Builder's Table",
    description:
      "TECHMED's signature monthly live event — every Builder, one room. Teaching, reflection, questions answered, progress celebrated.",
  },
  {
    title: 'Podcast',
    description:
      '5 Minutes Forward — a daily habit of intentional growth. Discipline, consistency, resilience: the character behind the score.',
  },
];
---

<section class="section">
  <h2>What You'll Receive</h2>
  <div class="grid">
    {items.map((item) => (
      <div class="item reveal">
        <h3>{item.title}</h3>
        <p>{item.description}</p>
      </div>
    ))}
  </div>
</section>

<style>
  .section {
    max-width: 1000px;
    margin: 0 auto;
    padding: var(--space-24) var(--space-6);
  }

  h2 {
    font-size: clamp(1.75rem, 4vw, 2.5rem);
    font-weight: 800;
    letter-spacing: -0.03em;
    margin-bottom: var(--space-12);
    text-align: center;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-5);
  }

  .item {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    padding: var(--space-6);
    border-top: 2px solid var(--accent);
    transition: transform 200ms ease, box-shadow 200ms ease;
  }

  .item:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 28px rgba(0, 0, 0, 0.2);
  }

  .item h3 {
    font-size: 1.05rem;
    margin-bottom: var(--space-3);
  }

  .item p {
    font-size: 0.9rem;
    line-height: 1.7;
    color: var(--text-light);
  }

  @media (max-width: 900px) {
    .grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (max-width: 560px) {
    .grid {
      grid-template-columns: 1fr;
    }
  }
</style>
